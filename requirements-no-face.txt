# =============================================================================
# UPEA Vota — Dependencias SIN reconocimiento facial
# =============================================================================
# Usa este archivo cuando face_recognition/dlib no compile en la plataforma
# de despliegue (Render free tier, etc.).
#
# Instalación:
#   pip install -r requirements-no-face.txt
#
# NOTA: La funcionalidad de verificación biométrica NO estará disponible.
#       El sistema seguirá funcionando para votación sin biometría facial.
# =============================================================================

fastapi==0.115.6
uvicorn[standard]==0.34.0
supabase==2.11.0
python-dotenv==1.1.0
pydantic-settings==2.7.1
pydantic[email]==2.10.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-multipart==0.0.20
httpx==0.28.1
Pillow==11.1.0
numpy==2.2.2
