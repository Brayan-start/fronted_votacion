# =============================================================================
# UPEA Vota — Dependencias del Backend (Python)
# =============================================================================
# Instalación:
#   pip install -r requirements.txt
#
# Para entornos donde face_recognition/dlib no compilan (ej: Render free tier):
#   pip install -r requirements-no-face.txt
# =============================================================================

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Base de datos — Supabase (PostgreSQL vía SDK)
supabase==2.11.0

# Variables de entorno
python-dotenv==1.1.0
pydantic-settings==2.7.1

# Validación
pydantic[email]==2.10.4

# JWT y autenticación
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# Multipart (subida de archivos)
python-multipart==0.0.20

# Cliente HTTP (reCAPTCHA, descarga de imágenes)
httpx==0.28.1

# Procesamiento de imágenes
Pillow==11.1.0

# Visión artificial y biometría facial
# NOTA: dlib se instala automáticamente como dependencia de face_recognition.
# En Windows puede requerir: pip install dlib-bin
# Si el build falla en Render, comenta o elimina face_recognition.
numpy==2.2.2
opencv-python-headless==4.11.0.86
face_recognition==1.3.0
