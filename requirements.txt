# =============================================================================
# UPEA Vota — Dependencias del Backend (Python)
# =============================================================================
# Instalación:
#   pip install -r requirements.txt
#   pip install face_recognition==1.3.0 --no-deps
#
# Para entornos donde face_recognition/dlib no compilan (ej: Render free tier):
#   pip install -r requirements-no-face.txt
# =============================================================================

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Base de datos — Supabase (PostgreSQL vía SDK)
supabase==2.11.0

# Variables de entorno
python-dotenv==1.1.0
pydantic-settings==2.7.1

# Validación
pydantic[email]==2.10.4

# JWT y autenticación
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# Multipart (subida de archivos)
python-multipart==0.0.20

# Cliente HTTP (reCAPTCHA, descarga de imágenes)
httpx==0.28.1

# Procesamiento de imágenes
Pillow==11.1.0

# Visión artificial y biometría facial
# IMPORTANTE: usamos dlib-bin (wheel precompilado) en vez de dlib (que requiere
# compilar con CMake y falla/tarda demasiado en Render Free). face_recognition
# se instala con --no-deps en el buildCommand para que no jale el dlib real.
# Ver buildCommand en render.yaml.
# setuptools<81 es necesario porque face_recognition_models usa pkg_resources,
# que setuptools 82+ eliminó.
setuptools<81
dlib-bin==19.24.6
numpy==2.2.2
opencv-python-headless==4.11.0.86
face-recognition-models==0.3.0
requests